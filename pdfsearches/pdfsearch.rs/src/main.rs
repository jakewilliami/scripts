// run using
// cd /Users/jakeireland/scripts/pdfsearches/pdfsearchrs/src/ && cargo run main.rs

#[macro_use]
extern crate lopdf;

// #[macro_use]
// extern crate pdf_extract;
// extern crate lopdf;

use std::env;
use std::path::PathBuf;
use std::path;
use std::io::BufWriter;
use std::fs::File;
// use pdf_extract::*;
use lopdf::*;

fn main() {
    println!("Hello, world!");
}
